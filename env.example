# Environment Variables Configuration

# =============================================================================
# CARDANO BLOCKCHAIN
# =============================================================================
# Network: mainnet, testnet, preview
NEXT_PUBLIC_CARDANO_NETWORK=testnet

# Smart contract address for access control
NEXT_PUBLIC_CONTRACT_ADDRESS=addr1_your_contract_address_here

# Blockfrost API (for querying blockchain data)
NEXT_PUBLIC_BLOCKFROST_PROJECT_ID=your_blockfrost_project_id
BLOCKFROST_API_KEY=your_blockfrost_api_key

# =============================================================================
# IPFS / FILECOIN STORAGE
# =============================================================================
# Infura IPFS
NEXT_PUBLIC_IPFS_GATEWAY=https://ipfs.infura.io
IPFS_PROJECT_ID=your_infura_project_id
IPFS_PROJECT_SECRET=your_infura_project_secret

# Alternative: Web3.Storage
# WEB3_STORAGE_TOKEN=your_web3_storage_token

# =============================================================================
# LIT PROTOCOL
# =============================================================================
# Network: serrano (testnet), cayenne (mainnet)
NEXT_PUBLIC_LIT_NETWORK=serrano

# =============================================================================
# AI / MASUMI AGENTS
# =============================================================================
# Masumi Network Integration
# See: https://docs.masumi.network/documentation
# 
# IMPORTANT ARCHITECTURE (from Masumi docs):
# 1. Masumi Payment Service = Handles payments & identity (NOT agent invocation)
# 2. Your Agent Services = Separate services that do the actual AI work
# 3. Integration = Optional Masumi for payments, agents work independently
#
# Your agents are SEPARATE services that you deploy yourself.
# Masumi Payment Service is only for payments, identity, and registry.
#
# Example architecture:
# - Deploy your AI agent service (e.g., using CrewAI, AutoGen, etc.)
# - Register it in Masumi for payments/identity (optional)
# - Set the agent endpoint URLs below to where your agents are hosted

# Optional: Enable Masumi for payments & identity
MASUMI_ENABLED=false
MASUMI_PAYMENT_SERVICE_URL=https://masumi-payment-service-production-7a37.up.railway.app/api/v1
MASUMI_API_KEY=<your_masumi_api_key>

# Your registered agent IDs in Masumi (for payments/identity)
# These are used if you want to charge users through Masumi
EXPLAINER_AGENT_ID=<your_explainer_agent_id_from_masumi_admin>
APPOINTMENT_AGENT_ID=<your_appointment_agent_id_from_masumi_admin>
INSURANCE_AGENT_ID=<your_insurance_agent_id_from_masumi_admin>

# IMPORTANT: Your actual agent service endpoints
# These are where your AI agents are deployed and running
# 
# Your deployed agent services (base URLs - /start_job will be appended automatically):
EXPLAINER_AGENT_ENDPOINT=https://explain-ai-zjrj.onrender.com
APPOINTMENT_AGENT_ENDPOINT=https://medledger-ai.onrender.com
INSURANCE_AGENT_ENDPOINT=
#
# Note: The code automatically appends /start_job to these URLs
# Your agents expect POST requests to /start_job with this format:
# {
#   "identifierFromPurchaser": "unique-id",
#   "input_data": {
#     "user_request": "user's query",
#     "pincode": "optional",
#     "patient_info": { ... }
#   }
# }
#
# Leave empty to use simulated responses (development mode)

# Legacy AI APIs (optional fallback)
# OPENAI_API_KEY=your_openai_api_key
# OPENAI_ORGANIZATION=your_openai_org_id
# ANTHROPIC_API_KEY=your_anthropic_api_key

# =============================================================================
# DATABASE - SUPABASE POSTGRES (REQUIRED)
# =============================================================================
# Supabase PostgreSQL connection string
# Note: Special characters in password must be URL-encoded (e.g., * = %2A, @ = %40, : = %3A)
# Example: password "Medledger1*" becomes "Medledger1%2A"
DATABASE_URL=postgresql://postgres:Medledger1%2A@db.lcewspeexqncnahwjxcv.supabase.co:5432/postgres

# MongoDB (optional, if needed)
# MONGODB_URI=mongodb://localhost:27017/medledger

# =============================================================================
# AUTHENTICATION
# =============================================================================
# NextAuth.js secret
NEXTAUTH_SECRET=your_nextauth_secret_min_32_chars
NEXTAUTH_URL=http://localhost:3000

# =============================================================================
# API KEYS & SECRETS
# =============================================================================
# Application secret for signing tokens
APP_SECRET=your_application_secret_key

# Backend Server Port
PORT=4000

# Encryption key for sensitive data (legacy, if needed)
ENCRYPTION_KEY=your_encryption_key_32_bytes

# =============================================================================
# EXTERNAL SERVICES
# =============================================================================
# Email service (e.g., SendGrid, AWS SES)
# EMAIL_API_KEY=your_email_service_api_key
# EMAIL_FROM=noreply@medledger.ai

# SMS service (e.g., Twilio)
# TWILIO_ACCOUNT_SID=your_twilio_account_sid
# TWILIO_AUTH_TOKEN=your_twilio_auth_token

# =============================================================================
# BACKBLAZE B2 STORAGE (for Patient Records)
# =============================================================================
# Backblaze B2 credentials for file storage
# Get these from: https://secure.backblaze.com/user_signin.htm
# 1. Create a B2 bucket
# 2. Create an Application Key with Read/Write/Delete permissions
B2_ACCOUNT_ID=your_b2_key_id
B2_APPLICATION_KEY=your_b2_application_key
B2_BUCKET_NAME=medledger-patient-records
B2_BUCKET_ID=your_b2_bucket_id

# =============================================================================
# MONITORING & ANALYTICS
# =============================================================================
# Sentry for error tracking
# SENTRY_DSN=your_sentry_dsn

# Google Analytics
# NEXT_PUBLIC_GA_ID=your_google_analytics_id

# =============================================================================
# FEATURE FLAGS
# =============================================================================
NEXT_PUBLIC_ENABLE_AI_FEATURES=true
NEXT_PUBLIC_ENABLE_INSURANCE_AUTOMATION=true
NEXT_PUBLIC_ENABLE_DEMO_MODE=false

# =============================================================================
# API URL (for frontend)
# =============================================================================
NEXT_PUBLIC_API_URL=http://localhost:4000
